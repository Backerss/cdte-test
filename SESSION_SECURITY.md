# 🔐 ระบบ Session Management และ Security

## ภาพรวมระบบ

ระบบจัดการ Session ที่มีความปลอดภัยสูง พร้อมฟีเจอร์ "Remember Me" และการป้องกันการโจมตีหลายรูปแบบ

---

## ⏰ กำหนดเวลา Session

### 1. Session ปกติ (Default)
- **ระยะเวลา**: 48 ชั่วโมง (2 วัน)
- **เงื่อนไข**: หากไม่มีกิจกรรมเกิน 2 ชั่วโมง จะถูกบังคับออกจากระบบ
- **การใช้งาน**: เหมาะสำหรับการเข้าใช้ปกติ

### 2. Remember Me (จดจำการเข้าสู่ระบบ)
- **ระยะเวลา**: 15 วัน
- **เงื่อนไข**: ไม่มีการตรวจสอบ Inactive Time
- **การใช้งาน**: ผู้ใช้ tick checkbox "จดจำฉันไว้" ตอนเข้าสู่ระบบ

---

## 🛡️ ระบบความปลอดภัย

### 1. Session Expiration
```javascript
// การตรวจสอบอายุ Session
const sessionMaxAge = rememberMe 
  ? 15 * 24 * 60 * 60 * 1000  // 15 วัน
  : 48 * 60 * 60 * 1000;       // 48 ชั่วโมง

if (sessionAge > sessionMaxAge) {
  // บังคับออกจากระบบ
  session.destroy();
  redirect('/login?error=session_expired');
}
```

### 2. Inactive Timeout
```javascript
// ตรวจสอบการไม่มีกิจกรรม (เฉพาะ non-remember-me)
const maxInactiveTime = 2 * 60 * 60 * 1000; // 2 ชั่วโมง

if (!rememberMe && inactiveTime > maxInactiveTime) {
  session.destroy();
  redirect('/login?error=session_inactive');
}
```

### 3. Session Fingerprinting (ป้องกัน Session Hijacking)

#### User-Agent Detection
```javascript
// บันทึก Browser Fingerprint ตอน Login
session.userAgent = req.headers['user-agent'];

// ตรวจสอบทุกครั้งที่เข้าใช้งาน
if (currentUserAgent !== session.userAgent) {
  // ตรวจพบการโจมตี - ยกเลิก session ทันที
  session.destroy();
  redirect('/login?error=security_violation');
}
```

#### IP Address Monitoring
```javascript
// บันทึก IP Address ตอน Login
session.ipAddress = req.ip;

// ตรวจสอบการเปลี่ยน IP (แจ้งเตือนแต่ไม่บังคับออก)
if (currentIP !== session.ipAddress) {
  logWarning('IP Address changed');
  session.ipAddress = currentIP; // อัปเดต IP ใหม่
}
```

### 4. Cookie Security
```javascript
cookie: {
  secure: false,           // ตั้ง true ใน production (HTTPS)
  httpOnly: true,          // ป้องกัน XSS
  sameSite: 'strict',      // ป้องกัน CSRF
  maxAge: dynamicMaxAge    // ปรับตามการ remember me
}
```

### 5. Rolling Session
```javascript
rolling: true  // ต่ออายุ session ทุกครั้งที่ user ใช้งาน
```

---

## 📝 การบันทึก Activity Log

### Login Activity
```javascript
await logActivity(
  'login',
  'เข้าสู่ระบบ',
  `${userName} (${role}) เข้าสู่ระบบ${rememberMe ? ' (จดจำฉัน)' : ''}`,
  userId,
  userName,
  { 
    role,
    rememberMe,
    ipAddress,
    userAgent
  }
);
```

### System Log
```javascript
await logSystem(
  'info', 
  'auth', 
  `User logged in: ${userId} [Remember Me: ${rememberMe}]`,
  userId
);
```

---

## 🔄 Flow การทำงาน

### Login Process
```
1. User กรอก Username + Password
2. ✅ เลือก "จดจำฉันไว้" (Optional)
3. Server Validate Credentials
4. สร้าง Session พร้อม:
   - userId, user data
   - createdAt, lastActivity
   - rememberMe flag
   - userAgent, ipAddress
5. ตั้งค่า Cookie maxAge ตาม rememberMe
6. บันทึก Activity Log
7. ↪️ Redirect to Dashboard
```

### Request Process
```
1. User เข้าถึงหน้าใดๆ
2. Middleware: checkSessionSecurity()
   ├─ ✅ ตรวจสอบ User-Agent
   ├─ ⚠️ ตรวจสอบ IP Address
   └─ ✅ อัปเดต lastActivity
3. Middleware: requireAuth()
   ├─ ❌ ไม่มี Session? → Redirect to Login
   ├─ ❌ Session หมดอายุ? → Logout + Redirect
   ├─ ❌ Inactive นานเกินไป? → Logout + Redirect
   └─ ✅ ผ่านการตรวจสอบ → Allow Access
4. ✅ แสดงหน้าที่ต้องการ
```

### Logout Process
```
1. User คลิกออกจากระบบ
2. บันทึก Activity Log
3. ทำลาย Session
4. ลบ Cookie
5. ↪️ Redirect to Login
```

---

## ⚠️ การแจ้งเตือนผู้ใช้

### Session Expired
```
❗ หมดเวลาเข้าใช้งาน
เซสชันของคุณหมดอายุแล้ว กรุณาเข้าสู่ระบบอีกครั้ง
```

### Session Inactive
```
ℹ️ หมดเวลาไม่มีกิจกรรม
คุณไม่มีกิจกรรมนานเกิน 2 ชั่วโมง กรุณาเข้าสู่ระบบอีกครั้ง
```

### Security Violation
```
🚨 ตรวจพบการเข้าถึงที่น่าสงสัย
เซสชันของคุณถูกยกเลิกเพื่อความปลอดภัย กรุณาเข้าสู่ระบบอีกครั้ง
```

---

## 🎯 ข้อดีของระบบ

### ✅ ความปลอดภัย
- ป้องกัน Session Hijacking
- ป้องกัน Session Fixation
- ป้องกัน XSS
- ป้องกัน CSRF
- IP Address Monitoring
- Browser Fingerprinting

### ✅ User Experience
- Remember Me สะดวกสำหรับผู้ใช้ปกติ
- Auto Logout ป้องกันการเข้าใช้โดยไม่ได้รับอนุญาต
- แจ้งเตือนชัดเจนเมื่อ session หมดอายุ

### ✅ Monitoring & Audit
- บันทึก Activity Log ทุกการ Login/Logout
- บันทึก System Log เพื่อการวิเคราะห์
- ตรวจจับพฤติกรรมผิดปกติ

---

## 🔧 Configuration

### Environment Variables (แนะนำ)
```env
SESSION_SECRET=your-super-secret-key-here
SESSION_MAX_AGE=172800000       # 48 hours in ms
REMEMBER_ME_MAX_AGE=1296000000  # 15 days in ms
INACTIVE_TIMEOUT=7200000        # 2 hours in ms
```

### Session Config (app.js)
```javascript
session({
  secret: process.env.SESSION_SECRET,
  name: 'cdte.sid',
  cookie: {
    secure: process.env.NODE_ENV === 'production',
    httpOnly: true,
    sameSite: 'strict',
    maxAge: 48 * 60 * 60 * 1000
  },
  rolling: true
})
```

---

## 📊 Firestore Collections

### system_activities
```javascript
{
  type: 'login|logout',
  title: 'เข้าสู่ระบบ',
  description: 'ชื่อผู้ใช้ (บทบาท) เข้าสู่ระบบ',
  userId: '6501001',
  userName: 'สมชาย ใจดี',
  timestamp: Timestamp,
  metadata: {
    role: 'student',
    rememberMe: true,
    ipAddress: '192.168.1.1',
    userAgent: 'Mozilla/5.0...'
  }
}
```

### system_logs
```javascript
{
  level: 'info|warning|error',
  category: 'auth',
  message: 'User logged in: 6501001 [Remember Me: true]',
  userId: '6501001',
  timestamp: Timestamp
}
```

---

## 🧪 การทดสอบ

### Test Case 1: Session Expiration
```
1. Login โดยไม่ tick Remember Me
2. รอ 48 ชั่วโมง
3. ✅ ต้องถูกบังคับออกจากระบบ
```

### Test Case 2: Inactive Timeout
```
1. Login โดยไม่ tick Remember Me
2. ไม่ทำอะไรเลย 2 ชั่วโมง
3. ✅ ต้องถูกบังคับออกจากระบบ
```

### Test Case 3: Remember Me
```
1. Login และ tick Remember Me
2. รอ 15 วัน
3. ✅ ยังสามารถใช้งานได้
4. รอเกิน 15 วัน
5. ✅ ต้องถูกบังคับออกจากระบบ
```

### Test Case 4: Session Hijacking
```
1. Login บน Browser A
2. คัดลอก Cookie ไปใช้ใน Browser B (ต่าง User-Agent)
3. ✅ Browser B ต้องถูกปฏิเสธและบังคับออกจากระบบ
```

### Test Case 5: Rolling Session
```
1. Login โดยไม่ tick Remember Me
2. ใช้งานอย่างต่อเนื่อง (ภายใน 2 ชั่วโมง)
3. ✅ Session ต้องถูกต่ออายุเรื่อยๆ
4. ✅ สามารถใช้งานได้เกิน 48 ชั่วโมง (ถ้ามีกิจกรรมต่อเนื่อง)
```

---

## 🚀 Production Checklist

- [ ] เปลี่ยน `SESSION_SECRET` เป็นค่าที่ซับซ้อนและปลอดภัย
- [ ] ตั้ง `cookie.secure = true` (ใช้ HTTPS)
- [ ] ตั้ง `NODE_ENV=production`
- [ ] ตรวจสอบ Firestore Security Rules
- [ ] ตั้งค่า Rate Limiting สำหรับ Login
- [ ] เปิดใช้งาน HTTPS Certificate
- [ ] ทดสอบทุก Test Case
- [ ] ตรวจสอบ Activity Log ทำงานถูกต้อง

---

## 📖 การใช้งาน

### สำหรับผู้ใช้ทั่วไป
1. เข้าสู่ระบบปกติ = Session 48 ชั่วโมง
2. ใช้งานต่อเนื่อง = Session ต่ออายุอัตโนมัติ
3. ไม่ใช้งาน > 2 ชั่วโมง = ถูกบังคับออก

### สำหรับผู้ที่ต้องการความสะดวก
1. เข้าสู่ระบบและ tick "จดจำฉันไว้"
2. Session 15 วัน
3. ไม่มีการตรวจสอบ Inactive Time

### สำหรับ Admin
1. ตรวจสอบ Activity Log ใน Dashboard > จัดการระบบ
2. ดู System Log สำหรับการ Debug
3. ติดตาม Security Alerts

---

## 🆘 Troubleshooting

### ปัญหา: ถูกบังคับออกบ่อยเกินไป
**แก้ไข**: ให้ tick "จดจำฉันไว้" ตอน Login

### ปัญหา: Security Violation แม้ใช้ Browser เดิม
**สาเหตุ**: Browser Extension อาจเปลี่ยน User-Agent
**แก้ไข**: ปิด Extension หรือใช้ Incognito Mode

### ปัญหา: IP Address Warning ตลอดเวลา
**สาเหตุ**: ใช้ Mobile Network (IP เปลี่ยนบ่อย)
**แก้ไข**: ระบบจะอัปเดต IP อัตโนมัติ ไม่ต้องกังวล

---

## 📞 Support

หากพบปัญหาหรือมีคำถาม:
1. ตรวจสอบ Activity Log
2. ตรวจสอบ System Log
3. ติดต่อผู้ดูแลระบบ

---

**Version**: 1.0.0  
**Last Updated**: December 1, 2025  
**Security Level**: ⭐⭐⭐⭐⭐ (High)
