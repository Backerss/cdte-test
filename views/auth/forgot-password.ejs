<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ลืมรหัสผ่าน - CDTE</title>
  <link rel="stylesheet" href="/css/style.css">
  <!-- SweetAlert2 CDN -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11.7.32/dist/sweetalert2.min.css">
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11.7.32/dist/sweetalert2.all.min.js"></script>
  <style>
    .icon{display:inline-block;width:20px;text-align:center}
  </style>
</head>
<body>
  <div class="auth-container">
    <div class="auth-card">
      <div class="auth-header">
        <h1>ลืมรหัสผ่าน</h1>
        <p>กรอกอีเมลสถานศึกษาเพื่อรีเซ็ตรหัสผ่าน</p>
      </div>

      <form id="forgotPasswordForm" method="POST" action="/forgot-password">
        <div class="form-group">
          <label for="email" class="form-label">อีเมลสถานศึกษา</label>
          <div class="input-with-icon">
            <span class="icon">✉️</span>
            <input 
              type="email" 
              id="email" 
              name="email" 
              class="form-input" 
              placeholder="example@nsru.ac.th"
              required
              pattern="[a-zA-Z0-9._%+-]+@nsru\.ac\.th$"
              title="กรุณากรอกอีเมล @nsru.ac.th เท่านั้น"
            >
          </div>
          <small class="text-muted" style="font-size:0.85rem;display:block;margin-top:6px">
            * ใช้อีเมลที่ลงท้ายด้วย @nsru.ac.th เท่านั้น
          </small>
        </div>

        <button type="submit" class="btn btn--primary btn--full">
          <span>ส่งลิงก์รีเซ็ตรหัสผ่าน</span>
        </button>
      </form>

      <div class="auth-footer">
        จำรหัสผ่านได้แล้ว? <a href="/login">เข้าสู่ระบบ</a>
      </div>
    </div>
  </div>

  <script>
    const form = document.getElementById('forgotPasswordForm');
    const emailInput = document.getElementById('email');

    form.addEventListener('submit', async function(e) {
      e.preventDefault();

      const email = emailInput.value.trim().toLowerCase();

      // Client-side validation
      if (!email) {
        Swal.fire({
          icon: 'error',
          title: 'กรุณากรอกข้อมูล',
          text: 'กรุณากรอกอีเมลสถานศึกษา',
          confirmButtonColor: '#2E3094'
        });
        return;
      }

      if (!email.endsWith('@nsru.ac.th')) {
        Swal.fire({
          icon: 'error',
          title: 'อีเมลไม่ถูกต้อง',
          text: 'กรุณาใช้อีเมลสถานศึกษาที่ลงท้ายด้วย @nsru.ac.th',
          confirmButtonColor: '#2E3094'
        });
        return;
      }

      // Show loading
      Swal.fire({
        title: 'กำลังส่งอีเมล...',
        allowOutsideClick: false,
        didOpen: () => {
          Swal.showLoading();
        }
      });

      // Submit form via fetch
      try {
        const response = await fetch('/forgot-password', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json'
          },
          body: JSON.stringify({ email })
        });

        const data = await response.json();

        if (response.ok) {
          await Swal.fire({
            icon: 'success',
            title: 'ส่งอีเมลสำเร็จ!',
            html: `ลิงก์รีเซ็ตรหัสผ่านถูกส่งไปที่<br><strong>${email}</strong><br><br><small class="text-muted">ลิงก์จะหมดอายุภายใน 30 นาที</small>`,
            confirmButtonColor: '#16A34A'
          });
          emailInput.value = '';
        } else {
          Swal.fire({
            icon: 'error',
            title: 'เกิดข้อผิดพลาด',
            text: data.message || 'ไม่พบอีเมลนี้ในระบบ',
            confirmButtonColor: '#DC2626'
          });
        }
      } catch (error) {
        Swal.fire({
          icon: 'error',
          title: 'เกิดข้อผิดพลาด',
          text: 'ไม่สามารถเชื่อมต่อกับเซิร์ฟเวอร์ได้',
          confirmButtonColor: '#DC2626'
        });
      }
    });
  </script>
</body>
</html>
