<%
  // Check if user is pure admin (admin role without student ID)
  const isPureAdmin = user && user.role === 'admin' && !user.studentId;
%>

<div class="page-content">
  <div class="card">
    <h2 style="margin-top:0;color:var(--color-primary);display:flex;align-items:center;gap:12px">
      <span>👤</span>
      โปรไฟล์
    </h2>
    <p class="text-muted">จัดการข้อมูลส่วนตัวของคุณ</p>

    <!-- Profile form for all users -->

    <form id="profileForm" class="dashboard-form" style="margin-top:24px">
      <!-- Profile Picture Section -->
      <div style="text-align:center;margin-bottom:32px">
        <div style="position:relative;display:inline-block">
          <img id="profileImage" 
            src="<%= user.avatar || '/img/default-avatar.svg' %>" 
               alt="Profile" 
               style="width:120px;height:120px;border-radius:50%;object-fit:cover;border:4px solid var(--color-primary)"
            onerror="this.src='/img/default-avatar.svg'">
          <input type="file" id="avatarInput" accept="image/*" style="display:none">
        </div>
        <div style="margin-top:16px">
          <button type="button" class="btn btn--secondary" onclick="document.getElementById('avatarInput').click()" style="font-size:0.9rem">
            เปลี่ยนรูปโปรไฟล์
          </button>
          <p style="margin:8px 0 0 0;font-size:0.85rem;color:var(--color-muted)">
            ⚠️ ใช้รูปภาพที่เห็นใบหน้าชัดเจนเท่านั้น ห้ามใช้รูปบุคคลอื่น (ไม่เกิน 10MB)
          </p>
        </div>
      </div>

      <!-- Personal Information Section -->
      <h3 style="color:var(--color-text);margin-bottom:16px;padding-bottom:8px;border-bottom:2px solid var(--color-border)">
        ข้อมูลส่วนตัว
      </h3>

      <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-bottom:16px">
        <div class="form-group">
          <label class="form-label">ชื่อ <span style="color:var(--color-danger)">*</span></label>
          <input type="text" id="firstNameInput" class="form-input" value="<%= user.firstName || '' %>" placeholder="กรอกชื่อ" required>
        </div>
        <div class="form-group">
          <label class="form-label">นามสกุล <span style="color:var(--color-danger)">*</span></label>
          <input type="text" id="lastNameInput" class="form-input" value="<%= user.lastName || '' %>" placeholder="กรอกนามสกุล" required>
        </div>
      </div>

      <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-bottom:16px">
        <div class="form-group">
          <label class="form-label" id="studentIdLabel">รหัสนักศึกษา</label>
          <input type="text" id="studentIdInput" class="form-input" value="<%= user.studentId || user.staffCode || '' %>" disabled style="background-color:var(--color-bg);cursor:not-allowed">
          <small id="studentIdHint" style="color:var(--color-muted);font-size:0.85rem">ไม่สามารถแก้ไขได้</small>
        </div>
        <div class="form-group">
          <label class="form-label">คณะ</label>
          <input type="text" class="form-input" value="คณะครุศาสตร์" disabled style="background-color:var(--color-bg);cursor:not-allowed">
          <small style="color:var(--color-muted);font-size:0.85rem">ไม่สามารถแก้ไขได้</small>
        </div>
      </div>

      <div class="form-group" style="margin-bottom:16px">
        <label class="form-label" id="majorLabel">สาขาวิชา <span id="majorRequired" style="color:var(--color-danger)">*</span></label>
        <select id="majorSelect" class="form-input" required style="cursor:pointer">
          <option value="">-- เลือกสาขาวิชา/ตำแหน่ง --</option>
          <option value="computer-education" <%= user.major === 'computer-education' ? 'selected' : '' %>>คอมพิวเตอร์ศึกษา</option>
          <option value="digital-technology-education" <%= user.major === 'digital-technology-education' ? 'selected' : '' %>>เทคโนโลยีดิจิทัลเพื่อการศึกษา</option>
          <option value="admin" <%= user.major === 'admin' ? 'selected' : '' %>>ผู้ดูแลระบบ</option>
          <option value="teacher" <%= user.major === 'teacher' ? 'selected' : '' %>>อาจารย์</option>
          <option value="staff" <%= user.major === 'staff' ? 'selected' : '' %>>บุคลากร</option>
        </select>
      </div>

      <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-bottom:16px">
        <div class="form-group">
          <label class="form-label" id="yearLabel">ชั้นปี</label>
          <input type="text" id="yearInput" class="form-input" value="<%= user.year || '' %>" disabled style="background-color:var(--color-bg);cursor:not-allowed">
        </div>
        <div class="form-group">
          <label class="form-label" id="roomLabel">ห้อง <span id="roomRequired" style="color:var(--color-danger)">*</span></label>
          <select id="roomSelect" class="form-input" style="cursor:pointer">
            <option value="">-- เลือกห้อง --</option>
            <option value="1">1</option>
            <option value="2">2</option>
            <option value="3">3</option>
            <option value="4">4</option>
            <option value="5">5</option>
            <option value="6">6</option>
            <option value="7">7</option>
            <option value="8">8</option>
            <option value="9">9</option>
            <option value="10">10</option>
          </select>
          <small id="roomHint" style="color:var(--color-muted);font-size:0.85rem">นักศึกษาสามารถเลือกห้องได้ครั้งเดียว</small>
        </div>
      </div>

      <div class="form-group" style="margin-bottom:16px">
        <label class="form-label">อีเมล <span style="color:var(--color-danger)">*</span></label>
        <input type="email" id="emailInput" class="form-input" value="<%= user.email || '' %>" placeholder="email@nsru.ac.th" required>
        <div id="emailWarning" style="display:none;margin-top:8px;padding:12px;background:#fff3cd;border-left:4px solid #ffc107;border-radius:4px">
          <strong style="color:#856404">⚠️ คำเตือน:</strong>
          <span style="color:#856404;font-size:0.9rem">นี่คือ Email นอกสถานศึกษาที่มหาวิทยาลัยไม่ได้รับรอง แนะนำให้ใช้ Email @nsru.ac.th</span>
        </div>
      </div>

      <div class="form-group" style="margin-bottom:16px">
        <label class="form-label">เบอร์โทรศัพท์</label>
        <input type="tel" id="phoneInput" class="form-input" value="<%= user.phone || '' %>" placeholder="0X-XXXX-XXXX" pattern="0[689]\d{8}">
        <small style="color:var(--color-muted);font-size:0.85rem">รูปแบบ: 0X-XXXX-XXXX</small>
      </div>

      <hr style="border:none;border-top:1px solid var(--color-border);margin:32px 0">

      <!-- Change Password Section -->
      <h3 style="color:var(--color-text);margin-bottom:16px;padding-bottom:8px;border-bottom:2px solid var(--color-border)">
        เปลี่ยนรหัสผ่าน
      </h3>

      <div class="form-group" style="margin-bottom:16px">
        <label class="form-label">รหัสผ่านปัจจุบัน</label>
        <input type="password" id="currentPasswordInput" class="form-input" placeholder="กรอกรหัสผ่านปัจจุบัน">
      </div>

      <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-bottom:16px">
        <div class="form-group">
          <label class="form-label">รหัสผ่านใหม่</label>
          <input type="password" id="newPasswordInput" class="form-input" placeholder="กรอกรหัสผ่านใหม่" minlength="8">
          <small style="color:var(--color-muted);font-size:0.85rem">ความยาวอย่างน้อย 8 ตัวอักษร</small>
        </div>
        <div class="form-group">
          <label class="form-label">ยืนยันรหัสผ่าน</label>
          <input type="password" id="confirmPasswordInput" class="form-input" placeholder="ยืนยันรหัสผ่านใหม่">
        </div>
      </div>

      <div style="display:flex;gap:12px;margin-top:24px">
        <button type="submit" class="btn btn--primary">
          💾 บันทึกข้อมูล
        </button>
        <button type="button" class="btn" style="background-color:var(--color-bg);color:var(--color-text)" onclick="resetForm()">
          ยกเลิก
        </button>
      </div>
    </form>
  </div>
</div>

<!-- Image Crop Modal -->
<div id="cropModal" class="modal-overlay" style="display:none">
  <div class="modal-content" style="max-width:600px">
    <div class="modal-header">
      <h3>ครอบตัดรูปภาพ</h3>
      <button type="button" class="modal-close" onclick="closeCropModal()">×</button>
    </div>
    <div class="modal-body" style="padding:24px">
      <p style="margin:0 0 16px 0;color:var(--color-muted)">
        ⚠️ กรุณาเลือกพื้นที่ที่ต้องการ (ควรเป็นรูปใบหน้าที่ชัดเจน)
      </p>
      <div style="max-height:400px;overflow:hidden;background:#000;border-radius:8px">
        <img id="cropImage" style="max-width:100%;display:block">
      </div>
    </div>
    <div class="modal-footer">
      <button type="button" class="btn btn--secondary" onclick="closeCropModal()">ยกเลิก</button>
      <button type="button" class="btn btn--primary" onclick="applyCrop()">ใช้รูปนี้</button>
    </div>
  </div>
</div>

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.6.1/cropper.min.css">
<script src="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.6.1/cropper.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

<style>
.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.7);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 9999;
  animation: fadeIn 0.2s ease;
}

.modal-content {
  background: var(--color-surface);
  border-radius: 12px;
  width: 90%;
  max-height: 90vh;
  overflow-y: auto;
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
  animation: slideUp 0.3s ease;
}

.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px 24px;
  border-bottom: 2px solid var(--color-border);
}

.modal-header h3 {
  margin: 0;
  color: var(--color-primary);
}

.modal-close {
  background: none;
  border: none;
  font-size: 1.8rem;
  cursor: pointer;
  color: var(--color-muted);
  padding: 4px;
  border-radius: 4px;
  transition: all 0.2s ease;
  line-height: 1;
}

.modal-close:hover {
  color: var(--color-danger);
  background: rgba(0, 0, 0, 0.05);
}

.modal-body {
  padding: 24px;
}

.modal-footer {
  display: flex;
  justify-content: flex-end;
  gap: 12px;
  padding: 20px 24px;
  border-top: 2px solid var(--color-border);
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
</style>

<script>
let cropper = null;
let croppedImageBlob = null;

// Email validation
const _emailInput = document.getElementById('emailInput');
const _emailWarning = document.getElementById('emailWarning');
if (_emailInput) {
  _emailInput.addEventListener('blur', function() {
    const email = this.value.trim();
    const warning = _emailWarning;
    if (warning) {
      if (email && !email.endsWith('@nsru.ac.th')) {
        warning.style.display = 'block';
      } else {
        warning.style.display = 'none';
      }
    }
  });
}

// Avatar upload with size check
const _avatarInput = document.getElementById('avatarInput');
if (_avatarInput) {
  _avatarInput.addEventListener('change', function(e) {
  const file = e.target.files[0];
  if (!file) return;

  // Check file size (10MB = 10 * 1024 * 1024 bytes)
  const maxSize = 10 * 1024 * 1024;
  if (file.size > maxSize) {
    Swal.fire({
      icon: 'error',
      title: 'ไฟล์ใหญ่เกินไป',
      text: 'กรุณาเลือกไฟล์ที่มีขนาดไม่เกิน 10MB',
      confirmButtonText: 'รับทราบ'
    });
    e.target.value = '';
    return;
  }

  // Check if it's an image
  if (!file.type.startsWith('image/')) {
    Swal.fire({
      icon: 'error',
      title: 'ไฟล์ไม่ถูกต้อง',
      text: 'กรุณาเลือกไฟล์รูปภาพเท่านั้น',
      confirmButtonText: 'รับทราบ'
    });
    e.target.value = '';
    return;
  }

  // Load image for cropping
    const reader = new FileReader();
    reader.onload = function(event) {
      const cropImg = document.getElementById('cropImage');
      if (cropImg) {
        cropImg.src = event.target.result;
        openCropModal();
      }
    };
    reader.readAsDataURL(file);
  });
}

function openCropModal() {
  const _cropModal = document.getElementById('cropModal');
  if (_cropModal) _cropModal.style.display = 'flex';

  // Initialize cropper
  const image = document.getElementById('cropImage');
  if (!image) return;
  if (cropper) {
    cropper.destroy();
  }
  cropper = new Cropper(image, {
    aspectRatio: 1,
    viewMode: 1,
    dragMode: 'move',
    autoCropArea: 0.8,
    restore: false,
    guides: true,
    center: true,
    highlight: false,
    cropBoxMovable: true,
    cropBoxResizable: true,
    toggleDragModeOnDblclick: false,
  });
}

function closeCropModal() {
  const _cropModal = document.getElementById('cropModal');
  if (_cropModal) _cropModal.style.display = 'none';
  if (cropper) {
    cropper.destroy();
    cropper = null;
  }
  const _avatar = document.getElementById('avatarInput');
  if (_avatar) _avatar.value = '';
}

function applyCrop() {
  if (!cropper) return;

  cropper.getCroppedCanvas({
    width: 300,
    height: 300,
    imageSmoothingEnabled: true,
    imageSmoothingQuality: 'high',
  }).toBlob(function(blob) {
    croppedImageBlob = blob;
    const url = URL.createObjectURL(blob);
    const _profileImage = document.getElementById('profileImage');
    if (_profileImage) _profileImage.src = url;
    closeCropModal();
    
    Swal.fire({
      icon: 'success',
      title: 'อัพโหลดรูปภาพสำเร็จ',
      text: 'กรุณากดบันทึกข้อมูลเพื่อยืนยันการเปลี่ยนแปลง',
      timer: 2000,
      showConfirmButton: false
    });
  }, 'image/jpeg', 0.9);
}

// (Submission handled by newer listener below)

function resetForm() {
  if (confirm('ต้องการยกเลิกการแก้ไขหรือไม่?')) {
    const _profileForm = document.getElementById('profileForm');
    if (_profileForm) _profileForm.reset();
    const _emailWarningRf = document.getElementById('emailWarning');
    if (_emailWarningRf) _emailWarningRf.style.display = 'none';
    location.reload();
  }
}

// Close modal when clicking outside
const _cropModalEl = document.getElementById('cropModal');
if (_cropModalEl) {
  _cropModalEl.addEventListener('click', function(e) {
    if (e.target === this) {
      closeCropModal();
    }
  });
}
</script>
<script>
// Load profile data from server and populate form
async function loadProfile() {
  try {
    const res = await fetch('/api/profile');
    const data = await res.json();
    if (!data.success) throw new Error(data.message || 'Failed to load');

    const user = data.data || {};

    // Populate fields
    const _firstNameInput = document.getElementById('firstNameInput');
    const _lastNameInput = document.getElementById('lastNameInput');
    const _phoneInput = document.getElementById('phoneInput');
    const _profileImageEl = document.getElementById('profileImage');
    const _majorSelect = document.getElementById('majorSelect');
    if (_firstNameInput) _firstNameInput.value = user.firstName || '';
    if (_lastNameInput) _lastNameInput.value = user.lastName || '';
    if (_phoneInput) _phoneInput.value = user.phone || '';
    if (_profileImageEl) _profileImageEl.src = user.avatar || '/img/default-avatar.svg';
    if (_majorSelect) _majorSelect.value = user.major || '';

    // Year (computed by server) and room
    const yearInput = document.getElementById('yearInput');
    const roomSelect = document.getElementById('roomSelect');
    const roomHint = document.getElementById('roomHint');
    if (yearInput) yearInput.value = user.year || '';
    if (roomSelect) {
      if (user.room) {
        roomSelect.value = user.room;
      } else {
        roomSelect.value = '';
      }
    }

    const emailInput = document.getElementById('emailInput');
    const emailWarning = document.getElementById('emailWarning');

    if (emailInput && emailWarning) {
      if (user.email && user.email.trim() !== '') {
        // Email already set — disable editing
        emailInput.value = user.email;
        emailInput.disabled = true;
        emailWarning.style.display = 'block';
        const strongEl = emailWarning.querySelector('strong');
        const spanEl = emailWarning.querySelector('span');
        if (strongEl) strongEl.textContent = '🔒 อีเมลล็อกแล้ว:';
        if (spanEl) spanEl.textContent = ' อีเมลแก้ไขได้เพียงครั้งเดียว หากต้องการเปลี่ยนจริง ๆ ติดต่อผู้ดูแลระบบ';
      } else {
        emailInput.value = '';
        emailInput.disabled = false;
        // reset the warning text if user types an external email
        const strongEl = emailWarning.querySelector('strong');
        const spanEl = emailWarning.querySelector('span');
        if (strongEl) strongEl.textContent = '⚠️ คำเตือน:';
        if (spanEl) spanEl.textContent = ' นี่คือ Email นอกสถานศึกษาที่มหาวิทยาลัยไม่ได้รับรอง แนะนำให้ใช้ Email @nsru.ac.th';
        emailWarning.style.display = 'none';
      }
    }

    // Adapt form for different user roles
    const studentIdLabel = document.getElementById('studentIdLabel');
    const studentIdInput = document.getElementById('studentIdInput');
    const studentIdHint = document.getElementById('studentIdHint');
    const majorLabel = document.getElementById('majorLabel');
    const majorRequired = document.getElementById('majorRequired');
    const yearLabel = document.getElementById('yearLabel');
    const roomLabel = document.getElementById('roomLabel');
    const roomRequired = document.getElementById('roomRequired');
    
    const isStudent = user.studentId && user.studentId.trim() !== '';
    const isAdmin = user.role === 'admin';
    const isTeacher = user.role === 'teacher';
    
    // Handle major/position field based on role
    const majorSelect = document.getElementById('majorSelect');
    
    if (isStudent) {
      // Student view - only show 2 academic majors
      if (studentIdLabel) studentIdLabel.textContent = 'รหัสนักศึกษา';
      if (studentIdInput) studentIdInput.value = user.studentId || '';
      if (studentIdHint) studentIdHint.textContent = 'ไม่สามารถแก้ไขได้';
      
      // Keep only student majors and make it required
      if (majorSelect) {
        majorSelect.innerHTML = `
          <option value="">-- เลือกสาขาวิชา --</option>
          <option value="computer-education" ${user.major === 'computer-education' ? 'selected' : ''}>คอมพิวเตอร์ศึกษา</option>
          <option value="digital-technology-education" ${user.major === 'digital-technology-education' ? 'selected' : ''}>เทคโนโลยีดิจิทัลเพื่อการศึกษา</option>
        `;
        majorSelect.disabled = false;
      }
      if (majorRequired) majorRequired.style.display = 'inline';
      if (majorLabel) majorLabel.innerHTML = 'สาขาวิชา <span id="majorRequired" style="color:var(--color-danger)">*</span>';
      
      // Room logic for students
      if (user.roomLocked) {
        if (roomSelect) roomSelect.disabled = true;
        if (roomHint) roomHint.textContent = 'ห้องถูกล็อกแล้ว (แก้ไขได้เพียงครั้งเดียว)';
      } else {
        if (roomSelect) roomSelect.disabled = false;
        if (roomHint) roomHint.textContent = 'นักศึกษาสามารถเลือกห้องได้ครั้งเดียว';
      }
    } else if (isTeacher) {
      // Teacher view - locked to teacher role
      if (studentIdLabel) studentIdLabel.textContent = 'รหัสอาจารย์';
      if (studentIdInput) studentIdInput.value = user.staffCode || '';
      if (studentIdHint) studentIdHint.textContent = 'รหัสประจำตัวอาจารย์';
      
      // Lock major to teacher
      if (majorSelect) {
        majorSelect.innerHTML = '<option value="teacher" selected>อาจารย์</option>';
        majorSelect.disabled = true;
        majorSelect.style.backgroundColor = 'var(--color-bg)';
        majorSelect.style.cursor = 'not-allowed';
      }
      if (majorRequired) majorRequired.style.display = 'none';
      if (majorLabel) majorLabel.innerHTML = 'ตำแหน่ง';
    } else if (isAdmin) {
      // Admin view - locked to admin role
      if (studentIdLabel) studentIdLabel.textContent = 'รหัสผู้ดูแล';
      if (studentIdInput) studentIdInput.value = user.staffCode || '';
      if (studentIdHint) studentIdHint.textContent = 'รหัสประจำตัวผู้ดูแลระบบ';
      
      // Lock major to admin
      if (majorSelect) {
        majorSelect.innerHTML = '<option value="admin" selected>ผู้ดูแลระบบ</option>';
        majorSelect.disabled = true;
        majorSelect.style.backgroundColor = 'var(--color-bg)';
        majorSelect.style.cursor = 'not-allowed';
      }
      if (majorRequired) majorRequired.style.display = 'none';
      if (majorLabel) majorLabel.innerHTML = 'ตำแหน่ง';
      
      // Year field becomes "ประสบการณ์" for admin/teacher
      if (yearLabel) yearLabel.textContent = 'ประสบการณ์ (ปี)';
      if (yearInput) {
        yearInput.disabled = false;
        yearInput.style.backgroundColor = '';
        yearInput.style.cursor = '';
        yearInput.placeholder = 'จำนวนปีประสบการณ์';
        yearInput.value = user.experience || user.year || '';
      }
      
      // Room becomes optional for admin/teacher
      if (roomRequired) roomRequired.style.display = 'none';
      if (roomLabel) roomLabel.textContent = 'ห้องทำงาน/สำนักงาน';
      if (roomHint) roomHint.textContent = 'ห้องที่ปฏิบัติงานหลัก (ไม่บังคับ)';
      if (roomSelect) {
        roomSelect.disabled = false;
        // Add more room options for staff
        const additionalOptions = [
          {value: 'admin', text: 'ห้องผู้บริหาร'},
          {value: 'office', text: 'สำนักงานคณะ'},
          {value: 'lab', text: 'ห้องปฏิบัติการ'},
          {value: 'other', text: 'อื่นๆ'}
        ];
        
        additionalOptions.forEach(opt => {
          const existingOption = Array.from(roomSelect.options).find(option => option.value === opt.value);
          if (!existingOption) {
            const option = document.createElement('option');
            option.value = opt.value;
            option.textContent = opt.text;
            roomSelect.appendChild(option);
          }
        });
      }
    }

  } catch (err) {
    console.error('Load profile error', err);
  }
}

loadProfile();

// All users can now edit their profile - role-specific adaptations are handled in loadProfile()

// Upload avatar helper - returns avatarUrl
async function uploadAvatarIfNeeded() {
  if (!croppedImageBlob) return null;

  const fd = new FormData();
  fd.append('avatar', croppedImageBlob, 'avatar.jpg');

  const resp = await fetch('/api/profile/avatar', { method: 'POST', body: fd });
  const json = await resp.json();
  if (!json.success) {
    throw new Error(json.message || 'Upload failed');
  }
  return json.avatarUrl;
}

// Submit profile
const _profileFormEl = document.getElementById('profileForm');
if (_profileFormEl) {
  _profileFormEl.addEventListener('submit', async function(e) {
  e.preventDefault();
  const firstNameEl = document.getElementById('firstNameInput');
  const lastNameEl = document.getElementById('lastNameInput');
  const majorEl = document.getElementById('majorSelect');
  const emailInput = document.getElementById('emailInput');
  const phoneEl = document.getElementById('phoneInput');
  const currentPasswordEl = document.getElementById('currentPasswordInput');
  const newPasswordEl = document.getElementById('newPasswordInput');
  const confirmPasswordEl = document.getElementById('confirmPasswordInput');

  const firstName = firstNameEl ? firstNameEl.value.trim() : '';
  const lastName = lastNameEl ? lastNameEl.value.trim() : '';
  const major = majorEl ? majorEl.value : '';
  const email = emailInput ? emailInput.value.trim() : '';
  const phone = phoneEl ? phoneEl.value.trim() : '';
  const currentPassword = currentPasswordEl ? currentPasswordEl.value : '';
  const newPassword = newPasswordEl ? newPasswordEl.value : '';
  const confirmPassword = confirmPasswordEl ? confirmPasswordEl.value : '';

  if (!firstName || !lastName) {
    Swal.fire({ icon: 'warning', title: 'กรุณากรอกชื่อและนามสกุล', confirmButtonText: 'รับทราบ' });
    return;
  }
  
  // Major validation - check if major is required based on form state
  const majorRequiredEl = document.getElementById('majorRequired');
  const isMajorRequired = majorRequiredEl && majorRequiredEl.style.display !== 'none';
  if (isMajorRequired && !major) {
    Swal.fire({ icon: 'warning', title: 'กรุณาเลือกสาขาวิชา', confirmButtonText: 'รับทราบ' });
    return;
  }

  // If email input is enabled, validate basic format
  if (!emailInput.disabled) {
    if (!email || !email.includes('@')) {
      Swal.fire({ icon: 'warning', title: 'อีเมลไม่ถูกต้อง', text: 'กรุณากรอกอีเมลที่ถูกต้อง', confirmButtonText: 'รับทราบ' });
      return;
    }
  }

  // Handle password change first (if requested)
  if (newPassword || confirmPassword) {
    if (!currentPassword) {
      Swal.fire({ icon: 'warning', title: 'กรุณากรอกรหัสผ่านปัจจุบัน', confirmButtonText: 'รับทราบ' });
      return;
    }
    if (newPassword !== confirmPassword) {
      Swal.fire({ icon: 'error', title: 'รหัสผ่านไม่ตรงกัน', confirmButtonText: 'รับทราบ' });
      return;
    }
    if (newPassword.length < 8) {
      Swal.fire({ icon: 'warning', title: 'รหัสผ่านสั้นเกินไป', text: 'รหัสผ่านใหม่ต้องอย่างน้อย 8 ตัวอักษร', confirmButtonText: 'รับทราบ' });
      return;
    }

    // Call change-password endpoint
    const pwResp = await fetch('/api/profile/change-password', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ currentPassword, newPassword })
    });
    const pwJson = await pwResp.json();
    if (!pwJson.success) {
      Swal.fire({ icon: 'error', title: 'เปลี่ยนรหัสผ่านไม่สำเร็จ', text: pwJson.message || 'เกิดข้อผิดพลาด', confirmButtonText: 'รับทราบ' });
      return;
    }
  }

  try {
    Swal.fire({ title: 'กำลังบันทึก...', allowOutsideClick: false, didOpen: () => Swal.showLoading() });

    // Upload avatar if there's a new cropped image
    let avatarUrl = null;
    if (croppedImageBlob) {
      avatarUrl = await uploadAvatarIfNeeded();
    }

    // Prepare payload
    const payload = { firstName, lastName, major, email, phone };
    const roomSelect = document.getElementById('roomSelect');
    if (roomSelect) {
      const roomVal = roomSelect.value;
      if (roomVal) payload.room = roomVal;
    }
    
    // Include year/experience data for all user types
    const yearInputEl = document.getElementById('yearInput');
    if (yearInputEl && yearInputEl.value.trim()) {
      payload.year = yearInputEl.value.trim();
    }
    
    if (avatarUrl) payload.avatar = avatarUrl;

    const res = await fetch('/api/profile', {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(payload)
    });

    const json = await res.json();
    Swal.close();
    if (json.success) {
      Swal.fire({ icon: 'success', title: 'บันทึกข้อมูลสำเร็จ', confirmButtonText: 'ตกลง' }).then(() => {
        // Clear password fields
        const _currentPw = document.getElementById('currentPasswordInput'); if (_currentPw) _currentPw.value = '';
        const _newPw = document.getElementById('newPasswordInput'); if (_newPw) _newPw.value = '';
        const _confPw = document.getElementById('confirmPasswordInput'); if (_confPw) _confPw.value = '';
        croppedImageBlob = null;
        loadProfile();
      });
    } else {
      Swal.fire({ icon: 'error', title: 'ไม่สามารถบันทึกข้อมูลได้', text: json.message || 'เกิดข้อผิดพลาด', confirmButtonText: 'รับทราบ' });
    }

  } catch (err) {
    console.error(err);
    Swal.fire({ icon: 'error', title: 'เกิดข้อผิดพลาด', text: err.message || '', confirmButtonText: 'รับทราบ' });
  }
});

} // end if _profileFormEl

// Disable email editing if it's already set - handled in loadProfile

</script>
