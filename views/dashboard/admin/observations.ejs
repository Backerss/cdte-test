<!-- External CSS -->
<link rel="stylesheet" href="/css/observations.css">

<div class="page-content">
  <div class="card">
    <div class="page-header">
      <div>
        <h2 style="margin-top:0;color:var(--color-primary);display:flex;align-items:center;gap:12px">
          <span>👁️‍🗨️</span>
          จัดการสังเกตุ
        </h2>
        <p class="text-muted">จัดการและติดตามการสังเกตการสอนของนักศึกษา</p>
      </div>
      <button class="btn btn--primary" onclick="openCreateObservationModal()">
        <span>➕</span>
        สร้างการสังเกตุใหม่
      </button>
    </div>

    <!-- Filter Section -->
    <div class="filter-section" style="margin:24px 0;padding:16px;background:var(--color-bg);border-radius:8px;">
      <div style="display:flex;gap:16px;align-items:end;flex-wrap:wrap;">
        <div class="form-group" style="margin:0;min-width:120px;">
          <label class="form-label">ปีการศึกษา</label>
          <select id="academicYearFilter" class="form-input" onchange="filterObservations()">
            <option value="">ทั้งหมด</option>
            <option value="2567">2567</option>
            <option value="2566">2566</option>
          </select>
        </div>
        <div class="form-group" style="margin:0;min-width:100px;">
          <label class="form-label">ชั้นปี</label>
          <select id="yearLevelFilter" class="form-input" onchange="filterObservations()">
            <option value="">ทั้งหมด</option>
            <option value="1">ปี 1</option>
            <option value="2">ปี 2</option>
            <option value="3">ปี 3</option>
            <option value="4">ปี 4</option>
          </select>
        </div>
        <div class="form-group" style="margin:0;min-width:120px;">
          <label class="form-label">สถานะ</label>
          <select id="statusFilter" class="form-input" onchange="filterObservations()">
            <option value="">ทั้งหมด</option>
            <option value="active">กำลังดำเนินการ</option>
            <option value="completed">เสร็จสิ้น</option>
            <option value="cancelled">ยกเลิก</option>
          </select>
        </div>
      </div>
    </div>

    <!-- Observations List -->
    <div id="observationsList">
      <!-- Content will be loaded here -->
    </div>
  </div>
</div>

<!-- Create Observation Modal -->
<div id="createObservationModal" class="modal-overlay" style="display:none">
  <div class="modal-content" style="max-width:700px;">
    <div class="modal-header">
      <h3>สร้างการสังเกตุใหม่</h3>
      <button type="button" class="modal-close" onclick="closeCreateObservationModal()">×</button>
    </div>
    
    <form id="createObservationForm">
      <div class="modal-body" style="padding:24px;">
        <div class="form-group">
          <label class="form-label">ชื่อการสังเกตุ <span class="required">*</span></label>
          <input type="text" class="form-input" id="observationName" placeholder="เช่น การสังเกตุการสอน ปี 2 เทอม 1/2567" required>
        </div>

        <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px;">
          <div class="form-group">
            <label class="form-label">ปีการศึกษา <span class="required">*</span></label>
            <select class="form-input" id="academicYear" onchange="checkYearConflict()" required>
              <option value="">เลือกปีการศึกษา</option>
              <option value="2567">2567</option>
              <option value="2568">2568</option>
            </select>
          </div>

          <div class="form-group">
            <label class="form-label">ชั้นปี <span class="required">*</span></label>
            <select class="form-input" id="yearLevel" onchange="checkYearConflict()" required>
              <option value="">เลือกชั้นปี</option>
              <option value="1">ปี 1</option>
              <option value="2">ปี 2</option>
              <option value="3">ปี 3</option>
              <option value="4">ปี 4</option>
            </select>
          </div>
        </div>

        <div id="yearConflictWarning" class="alert alert--danger" style="display:none;">
          <strong>⚠️ คำเตือน:</strong> มีการสังเกตุสำหรับชั้นปีนี้ในปีการศึกษานี้แล้ว กรุณาเลือกชั้นปีอื่น
        </div>

        <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px;">
          <div class="form-group">
            <label class="form-label">วันเริ่มต้น <span class="required">*</span></label>
            <input type="date" class="form-input" id="startDate" onchange="calculateEndDate()" required>
          </div>

          <div class="form-group">
            <label class="form-label">วันสิ้นสุด</label>
            <input type="date" class="form-input" id="endDate" style="background:#f5f5f5;">
            <small class="form-help">จะคำนวณอัตโนมัติ (15 วัน)</small>
          </div>
        </div>

        <div class="form-group">
          <label class="form-label">คำอธิบาย</label>
          <textarea class="form-input" id="description" rows="3" placeholder="รายละเอียดเพิ่มเติมเกี่ยวกับการสังเกตุ"></textarea>
        </div>

        <!-- Student Selection -->
        <div class="form-group">
          <label class="form-label">เลือกนักศึกษา</label>
          <div class="student-selection">
            <!-- Filter Section -->
            <div style="display:grid;grid-template-columns:1fr 1fr 2fr;gap:12px;margin-bottom:12px;">
              <select class="form-input" id="studentYearFilter" onchange="filterStudentsList()">
                <option value="">ทุกชั้นปี</option>
                <option value="1">ปี 1</option>
                <option value="2">ปี 2</option>
                <option value="3">ปี 3</option>
                <option value="4">ปี 4</option>
              </select>
              <select class="form-input" id="studentPrefixFilter" onchange="filterStudentsList()">
                <option value="">รหัสนำหน้าทั้งหมด</option>
                <!-- Options will be populated dynamically -->
              </select>
              <input type="text" class="form-input" id="studentSearch" placeholder="ค้นหานักศึกษา (รหัส, ชื่อ, นามสกุล)" onkeyup="filterStudentsList()">
            </div>
            
            <!-- Action Buttons -->
            <div style="display:flex;gap:8px;margin-bottom:12px;">
              <button type="button" class="btn btn--secondary btn--sm" onclick="selectAllVisibleStudents()">เลือกที่แสดง</button>
              <button type="button" class="btn btn--secondary btn--sm" onclick="clearAllStudents()">ล้างทั้งหมด</button>
              <span style="margin-left:auto;font-size:0.85rem;color:var(--color-muted);align-self:center;" id="selectedCount">เลือกแล้ว: 0 คน</span>
            </div>
            
            <div id="studentsList" class="students-list">
              <!-- Students will be loaded here -->
            </div>
          </div>
        </div>
      </div>

      <div class="modal-footer">
        <button type="button" class="btn btn--secondary" onclick="closeCreateObservationModal()">ยกเลิก</button>
        <button type="submit" class="btn btn--primary">สร้างการสังเกตุ</button>
      </div>
    </form>
  </div>
</div>

<!-- Student Management Modal -->
<div id="studentManagementModal" class="modal-overlay" style="display:none">
  <div class="modal-content" style="max-width:900px;">
    <div class="modal-header">
      <h3 id="managementModalTitle">จัดการนักศึกษา</h3>
      <button type="button" class="modal-close" onclick="closeStudentManagementModal()">×</button>
    </div>
    
    <div class="modal-body" style="padding:24px;">
      <div id="studentManagementContent">
        <!-- Content will be loaded here -->
      </div>
    </div>
  </div>
</div>

<!-- Export Modal -->
<div id="exportModal" class="modal-overlay" style="display:none">
  <div class="modal-content" style="max-width:500px;">
    <div class="modal-header">
      <h3>📄 ส่งออกข้อมูล</h3>
      <button type="button" class="modal-close" onclick="closeExportModal()">×</button>
    </div>
    
    <div class="modal-body" style="padding:24px;">
      <p style="margin-bottom:20px;color:var(--color-text);">เลือกรูปแบบการส่งออกข้อมูล</p>
      
      <div class="export-options" style="display:flex;flex-direction:column;gap:12px;">
        <button class="export-option-btn" onclick="exportStudentList()" style="width:100%;padding:16px;text-align:left;border:2px solid var(--color-border);border-radius:8px;background:white;cursor:pointer;transition:all 0.2s;">
          <div style="display:flex;align-items:center;gap:12px;">
            <span style="font-size:24px;">📋</span>
            <div>
              <div style="font-weight:600;color:var(--color-primary);font-size:1rem;">รายชื่อนักศึกษา</div>
              <div style="font-size:0.85rem;color:var(--color-muted);margin-top:4px;">เอกสารรายชื่อนักศึกษาในงวดการสังเกตุ (PDF)</div>
            </div>
          </div>
        </button>

        <button class="export-option-btn" onclick="showComingSoon()" style="width:100%;padding:16px;text-align:left;border:2px solid var(--color-border);border-radius:8px;background:#f5f5f5;cursor:not-allowed;transition:all 0.2s;" disabled>
          <div style="display:flex;align-items:center;gap:12px;">
            <span style="font-size:24px;opacity:0.5;">📊</span>
            <div>
              <div style="font-weight:600;color:var(--color-muted);font-size:1rem;">สรุปผลการประเมิน (เร็วๆ นี้)</div>
              <div style="font-size:0.85rem;color:var(--color-muted);margin-top:4px;">รายงานสรุปผลการประเมินและความคืบหน้า</div>
            </div>
          </div>
        </button>

        <button class="export-option-btn" onclick="showComingSoon()" style="width:100%;padding:16px;text-align:left;border:2px solid var(--color-border);border-radius:8px;background:#f5f5f5;cursor:not-allowed;transition:all 0.2s;" disabled>
          <div style="display:flex;align-items:center;gap:12px;">
            <span style="font-size:24px;opacity:0.5;">📈</span>
            <div>
              <div style="font-weight:600;color:var(--color-muted);font-size:1rem;">รายงานแผนการสอน (เร็วๆ นี้)</div>
              <div style="font-size:0.85rem;color:var(--color-muted);margin-top:4px;">รายงานสถานะการส่งแผนการสอน</div>
            </div>
          </div>
        </button>
      </div>
    </div>

    <div class="modal-footer">
      <button type="button" class="btn btn--secondary" onclick="closeExportModal()">ปิด</button>
    </div>
  </div>
</div>

<style>
.export-option-btn:not(:disabled):hover {
  border-color: var(--color-primary);
  background: var(--color-bg);
  transform: translateY(-2px);
  box-shadow: 0 4px 8px rgba(0,0,0,0.1);
}
</style>

<!-- Pass user data to JavaScript -->
<script>
  window.currentUser = {
    role: '<%= user.role %>'
  };
</script>

<!-- External JavaScript -->
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf-autotable/3.5.31/jspdf.plugin.autotable.min.js"></script>
<script src="/js/observations.js"></script>
<script src="/js/school-info.js"></script>