<%
  // Use server-provided evaluation practice history. Server should pass `evaluationPracticeHistory` and optionally `activeEvalPeriodId`.
  const evalPractice = (typeof evaluationPracticeHistory !== 'undefined' && Array.isArray(evaluationPracticeHistory))
    ? evaluationPracticeHistory
    : [];

  const activeEvalPeriod = (typeof activeEvalPeriodId !== 'undefined')
    ? evalPractice.find(p => p.id === activeEvalPeriodId)
    : evalPractice.find(p => p.status === 'active');

  const currentEvalData = activeEvalPeriod || (evalPractice.length > 0 ? evalPractice[evalPractice.length - 1] : null);
  const userYear = user.year || 3;

  const hasPractice = evalPractice.length > 0;
  const hasActive = !!activeEvalPeriod;

  // Check if user is pure admin (admin role without student ID)
  const userIdForCheck = user && (user.user_id || user.studentId);
  const isPureAdmin = user && user.role === 'admin' && (!userIdForCheck || /^A/.test(String(userIdForCheck)));

  // Page interactivity: enabled only when there's a practice period and an active one, and user is not a pure admin
  const interactiveEnabled = !isPureAdmin && hasPractice && hasActive;
%>

<div class="page-content">
  <div class="card">
    <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:24px;gap:16px;flex-wrap:wrap">
      <div>
        <h2 style="margin:0 0 8px 0;color:var(--color-primary);display:flex;align-items:center;gap:12px">
          <span>📝</span>
          การประเมินการสังเกตการสอน
        </h2>
        <p class="text-muted" style="margin:0">แบบประเมินการสังเกตการสอนระหว่างฝึกประสบการณ์วิชาชีพครู</p>
      </div>
      
      <div class="form-group" style="margin:0;min-width:300px">
        <label class="form-label">เลือกการฝึกประสบการณ์วิชาชีพครู</label>
        <select id="evalPeriodSelector" class="form-input" onchange="changeEvalPeriod()" style="padding:10px 14px">
          <% if (!hasPractice) { %>
            <option value="none" selected disabled>ยังไม่มีการฝึกประสบการณ์วิชาชีพครู</option>
          <% } else { %>
            <% evalPractice.slice().reverse().forEach((record) => { %>
              <option value="<%= record.id %>" <%= record.status === 'active' ? 'selected' : '' %>>
                <%= record.period %> (ปี <%= record.year %>)
                <%= record.status === 'active' ? ' - ปัจจุบัน' : '' %>
              </option>
            <% }); %>
          <% } %>
        </select>
      </div>
    </div>

    <% if (isPureAdmin) { %>
      <div style="background: linear-gradient(135deg, #ff6b6b 0%, #ee5a24 100%); color: white; padding: 20px; border-radius: 12px; margin-bottom: 24px; box-shadow: 0 4px 12px rgba(255, 107, 107, 0.3);">
        <div style="display: flex; align-items: center; gap: 12px; margin-bottom: 12px;">
          <span style="font-size: 1.5rem;">🔒</span>
          <h3 style="margin: 0; font-size: 1.2rem;">สำหรับผู้ดูแลระบบเท่านั้น</h3>
        </div>
        <p style="margin: 0; font-size: 1rem; line-height: 1.5; opacity: 0.95;">คุณเข้าสู่ระบบในฐานะ <strong>ผู้ดูแลระบบ</strong> ซึ่งไม่สามารถทำการประเมินได้<br>หากต้องการทำการประเมิน กรุณาเข้าสู่ระบบด้วยรหัสนักศึกษา</p>
      </div>
    <% } else if (!hasActive) { %>
      <div id="noEvalActiveBanner" style="background:#fff3cd;border-left:4px solid #ffc107;padding:12px;border-radius:8px;margin-bottom:16px;color:#856404">
        ⚠️ ยังไม่มีการฝึกประสบการณ์วิชาชีพครูที่ถูกตั้งค่าเป็น "ปัจจุบัน" — คุณไม่สามารถทำการประเมินได้จนกว่าจะมีการตั้งการฝึกประสบการณ์วิชาชีพครูที่ใช้งานอยู่
      </div>
    <% } %>

    <!-- แสดงประวัติการฝึกประสบการณ์วิชาชีพครูที่ผ่านมา (Read-only) -->
    <div id="evalHistoryView" style="display:none">
      <div style="background:rgba(251,180,37,0.1);border-left:4px solid var(--color-secondary);padding:16px;border-radius:8px;margin-bottom:24px">
        <div style="display:flex;align-items:center;gap:10px;margin-bottom:12px">
          <span style="font-size:1.3rem">📚</span>
          <strong style="color:var(--color-text);font-size:1.1rem">ข้อมูลย้อนหลัง (Read-Only)</strong>
        </div>
        <p style="margin:0;color:var(--color-muted);font-size:0.9rem">
          คุณกำลังดูข้อมูลการประเมินจากการฝึกประสบการณ์วิชาชีพครูที่ผ่านมาแล้ว ไม่สามารถแก้ไขได้
        </p>
      </div>
      <div id="evalHistoryContent"></div>
    </div>

    <!-- ส่วนสำหรับการฝึกประสบการณ์วิชาชีพครูปัจจุบัน (Editable) -->
    <% if (!isPureAdmin) { %>
    <div id="evalCurrentContent">
        <!-- Practice Period Info -->
        <div class="info-banner" style="background:var(--color-bg);border-left:4px solid var(--color-primary);padding:16px;border-radius:8px;margin-bottom:24px">
        <div style="display:flex;align-items:center;gap:12px;margin-bottom:8px">
          <span style="font-size:1.5rem">📅</span>
          <div>
            <strong style="color:var(--color-text)">ช่วงการฝึกประสบการณ์:</strong>
            <span id="practiceRange">-</span>
          </div>
        </div>
        <div style="margin:8px 0 0 44px;font-size:0.9rem;color:var(--color-muted)">
          <strong style="color:var(--color-text)">ชั้นปีที่ <%= userYear %>:</strong>
          <% if (userYear <= 3) { %>
            <span>ต้องประเมิน <strong>อย่างน้อย 1 ครั้งต่อสัปดาห์</strong> (รวม 3 สัปดาห์, แนะนำไม่เกิน 9 ครั้ง)</span>
          <% } else { %>
            <span style="color:var(--color-success)">✅ ไม่ต้องทำการประเมิน (ปี 4 ไม่มีการประเมินการสังเกตการสอน)</span>
          <% } %>
        </div>
        <% if (userYear <= 3) { %>
          <p style="margin:8px 0 0 44px;font-size:0.9rem;color:var(--color-muted)">
            * การประเมินสามารถทำได้เฉพาะภายในช่วงฝึกเท่านั้น | ไม่บังคับ 3 ครั้ง/สัปดาห์ แต่ต้องประเมินอย่างน้อย 1 ครั้งทุกสัปดาห์
          </p>
        <% } %>
      </div>

      <!-- Lesson Plan Upload Section (Separate Task) -->
      <!-- เฉพาะปี 2-3 เท่านั้นที่ต้องส่งแผนการจัดการเรียนรู้ -->
      <!-- ปี 1: ไม่ต้องส่ง (ยังไม่มีการลงสอนในโรงเรียน) -->
      <!-- ปี 2-3: ต้องส่งแผนการจัดการเรียนรู้ (มีการลงสอนจริง) -->
      <!-- ปี 4: ไม่มีการประเมินการสังเกตการสอน (ฝึกปฏิบัติการสอนเต็มเวลา) -->
      <% if (userYear >= 2 && userYear <= 3) { %>
      <div class="card" style="margin-bottom:24px;background:var(--color-bg);border:2px dashed var(--color-primary)">
        <h3 style="color:var(--color-primary);margin:0 0 16px 0;display:flex;align-items:center;gap:12px">
          <span>📋</span>
          การส่งแผนการจัดการเรียนรู้
          <span style="background:var(--color-warning);color:white;font-size:0.75rem;padding:4px 12px;border-radius:12px;font-weight:600">งานแยก</span>
        </h3>
        <p style="margin:0 0 16px 0;color:var(--color-muted)">
          นักศึกษาชั้นปีที่ <%= userYear %> กรุณาส่งแผนการจัดการเรียนรู้แยกต่างหากจากการประเมินรายสัปดาห์
        </p>

      <!-- Lesson Plan Upload UI -->
      <div style="background:white;padding:20px;border-radius:8px;border:1px solid var(--color-border)">
        <div style="display:flex;gap:12px;align-items:center;margin-bottom:16px">
          <input type="file" id="mainLessonPlanInput" accept=".pdf,.doc,.docx,.ppt,.pptx" style="display:none">
          <button type="button" class="btn btn--primary" onclick="document.getElementById('mainLessonPlanInput').click()" style="display:flex;align-items:center;gap:8px"
            <%= interactiveEnabled ? '' : 'disabled title="ไม่สามารถเลือกไฟล์ได้ โปรดตั้งการฝึกประสบการณ์วิชาชีพครูที่ใช้งานก่อน"' %>
          >
            <span>📎</span>
            เลือกไฟล์แผนการจัดการเรียนรู้
          </button>
          <span id="mainLessonPlanFileName" style="color:var(--color-muted);font-size:0.9rem">ยังไม่ได้เลือกไฟล์</span>
        </div>

        <div id="mainLessonPlanPreview" style="display:none;background:var(--color-bg);padding:16px;border-radius:8px;border:1px solid var(--color-border)">
          <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px">
            <strong style="color:var(--color-text)">ตัวอย่างไฟล์:</strong>
            <button type="button" class="btn btn--danger btn--icon" onclick="removeMainLessonPlan()" title="ลบไฟล์">
              🗑️
            </button>
          </div>
          <div id="mainLessonPlanPreviewContent"></div>
        </div>

        <div style="margin-top:16px;text-align:right">
          <button type="button" class="btn btn--success" onclick="submitLessonPlan()" id="submitLessonPlanBtn" disabled style="display:flex;align-items:center;gap:8px;margin-left:auto">
            <span>📤</span>
            ส่งแผนการจัดการเรียนรู้
          </button>
        </div>
      </div>
      
        <!-- Lesson Plan Status -->
        <div id="lessonPlanStatus" style="margin-top:16px;padding:12px;border-radius:8px;background:#fff3cd;color:#856404;display:none">
          <div style="display:flex;align-items:center;gap:8px">
            <span>📋</span>
            <span id="lessonPlanStatusText">สถานะ: รอส่งแผนการจัดการเรียนรู้</span>
          </div>
        </div>
      </div>
      <% } %>

      <!-- Video Upload Section (Year 3 Only) -->
      <% if (userYear === 3) { %>
      <div class="card" style="margin-bottom:24px;background:linear-gradient(135deg, rgba(46,48,148,0.05) 0%, rgba(251,180,37,0.05) 100%);border:2px solid var(--color-primary)">
        <h3 style="color:var(--color-primary);margin:0 0 16px 0;display:flex;align-items:center;gap:12px">
          <span>🎥</span>
          การส่งคลิปวิดีโอการสอน
          <span style="background:var(--color-danger);color:white;font-size:0.75rem;padding:4px 12px;border-radius:12px;font-weight:600">บังคับ - ปี 3</span>
        </h3>
        <p style="margin:0 0 16px 0;color:var(--color-muted)">
          นักศึกษาชั้นปีที่ 3 กรุณาอัพโหลดคลิปวิดีโอการสอนของคุณลงบน <strong>YouTube</strong> แล้วนำลิงก์มาวางที่นี่
        </p>

        <!-- Video URL Input UI -->
        <div style="background:white;padding:20px;border-radius:8px;border:1px solid var(--color-border)">
          <div class="form-group" style="margin-bottom:16px">
            <label class="form-label" style="font-weight:600;color:var(--color-text);margin-bottom:8px;display:block">
              🔗 ลิงก์ YouTube ของคลิปวิดีโอการสอน <span class="required">*</span>
            </label>
            <input 
              type="url" 
              id="videoLinkInput" 
              class="form-input" 
              placeholder="https://www.youtube.com/watch?v=xxxxx หรือ https://youtu.be/xxxxx"
              style="padding:12px;font-size:1rem"
            >
            <small style="display:block;margin-top:8px;color:var(--color-muted);font-size:0.85rem">
              💡 วิธีการ: อัพโหลดคลิปลง YouTube แล้วคัดลอกลิงก์มาวางที่นี่ | คลิปควรตั้งเป็น <strong>สาธารณะ</strong> หรือ <strong>Unlisted</strong>
            </small>
          </div>

          <!-- Video Preview -->
          <div id="videoPreview" style="display:none;margin-top:20px;padding:16px;background:var(--color-bg);border-radius:8px;border:1px solid var(--color-border)">
            <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px">
              <strong style="color:var(--color-text)">ตัวอย่างวิดีโอ:</strong>
              <button type="button" class="btn btn--danger btn--icon" onclick="removeVideoLink()" title="ลบลิงก์">
                🗑️
              </button>
            </div>
            <div id="videoPreviewContent" style="margin-top:12px"></div>
          </div>

          <div style="margin-top:16px;display:flex;gap:12px;align-items:center">
              <button type="button" class="btn btn--primary" onclick="validateVideoLink()" style="display:flex;align-items:center;gap:8px" <%= interactiveEnabled ? '' : 'disabled title="โปรดเลือกการฝึกประสบการณ์วิชาชีพครูที่ใช้งานก่อน"' %>>
                <span>✅</span>
                ตรวจสอบลิงก์
              </button>
              <button type="button" class="btn btn--success" onclick="submitVideoLink()" id="submitVideoBtn" <%= interactiveEnabled ? '' : 'disabled title="ไม่สามารถส่งได้ โปรดเลือกการฝึกประสบการณ์วิชาชีพครูที่ใช้งานก่อน"' %> style="display:flex;align-items:center;gap:8px">
                <span>📤</span>
                ส่งลิงก์วิดีโอ
              </button>
          </div>
        </div>
        
        <!-- Video Status -->
        <div id="videoStatus" style="margin-top:16px;padding:12px;border-radius:8px;background:#fff3cd;color:#856404;display:none">
          <div style="display:flex;align-items:center;gap:8px">
            <span>🎥</span>
            <span id="videoStatusText">สถานะ: รอส่งลิงก์วิดีโอ</span>
          </div>
        </div>
      </div>
      <% } %>

      <!-- Evaluation Grid: 3 weeks × 3 evaluations -->
      <% if (userYear <= 3) { %>
      <div style="margin-top:24px">
        <% for (let week = 1; week <= 3; week++) { %>
          <div class="eval-week-section" style="margin-bottom:32px">
            <h3 style="color:var(--color-primary);margin:0 0 16px 0;display:flex;align-items:center;gap:8px">
              <span>📌</span>
              สัปดาห์ที่ <%= week %>
            </h3>
            
            <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:16px">
              <% for (let evalNum = 1; evalNum <= 3; evalNum++) { 
                 const globalNum = (week - 1) * 3 + evalNum;
              %>
                <div class="eval-card" data-eval="<%= globalNum %>">
                  <div class="eval-header" style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px">
                    <h4 style="margin:0;color:var(--color-text)">ครั้งที่ <%= globalNum %></h4>
                    <span class="eval-status pending" id="status-<%= globalNum %>">
                      <span class="status-icon">⏳</span>
                      <span class="status-text">รอประเมิน</span>
                    </span>
                  </div>
                  
                  <div class="eval-date" id="date-<%= globalNum %>" style="font-size:0.85rem;color:var(--color-muted);margin-bottom:12px">
                    วันที่: <span class="date-value">-</span>
                  </div>

                          <button class="btn btn--primary btn-eval" 
                                  data-eval="<%= globalNum %>" 
                                  onclick="startEvaluation(<%= globalNum %>)"
                                  style="width:100%"
                                  <%= interactiveEnabled ? '' : 'disabled title="กรุณาเลือกการฝึกประสบการณ์วิชาชีพครูที่ใช้งานก่อน เพื่อเริ่มประเมิน"' %>
                                  >
                            เริ่มประเมิน
                          </button>
                </div>
              <% } %>
            </div>
          </div>
        <% } %>
      </div>
      <% } else { %>
        <div style="text-align:center;padding:60px 20px;background:var(--color-bg);border-radius:12px;border:2px dashed var(--color-border)">
          <div style="font-size:4rem;margin-bottom:16px">🎓</div>
          <h3 style="color:var(--color-primary);margin:0 0 12px 0">ชั้นปีที่ 4 ไม่มีการประเมินการสังเกตการสอน</h3>
          <p style="color:var(--color-muted);margin:0">คุณกำลังฝึกปฏิบัติการสอนในสถานศึกษาเต็มเวลา ไม่ต้องทำการประเมินส่วนนี้</p>
        </div>
      <% } %>
    </div>
    <% } %>
  </div>
</div>

<!-- Evaluation Details Modal (Read-only) -->
<div id="evaluationDetailsModal" class="modal-overlay" style="display:none">
  <div class="modal-content" style="max-width:1000px;max-height:90vh">
    <div class="modal-header">
      <h3>📊 รายละเอียดการประเมิน - <span id="detailsEvalTitle">ครั้งที่ 1</span></h3>
      <button type="button" class="modal-close" onclick="closeDetailsModal()">×</button>
    </div>
    
    <div class="modal-body" style="padding:24px">
      <div class="eval-info" style="background:var(--color-bg);padding:12px;border-radius:6px;margin-bottom:20px">
        <strong>วันที่ประเมิน:</strong> <span id="detailsEvalDate">-</span>
      </div>

      <div id="detailsContent"></div>
    </div>

    <div class="modal-footer">
      <button type="button" class="btn btn--secondary" onclick="closeDetailsModal()">
        ปิด
      </button>
    </div>
  </div>
</div>

<!-- Evaluation Modal -->
<div id="evaluationModal" class="modal-overlay" style="display:none">
  <div class="modal-content" style="max-width:900px;max-height:90vh">
    <div class="modal-header">
      <h3>แบบประเมินการสังเกตการสอน - <span id="evalTitle">ครั้งที่ 1</span></h3>
      <button type="button" class="modal-close" onclick="closeEvaluationModal()">×</button>
    </div>
    
    <div class="modal-body" style="padding:24px">
      <div class="eval-info" style="background:var(--color-bg);padding:12px;border-radius:6px;margin-bottom:20px">
        <strong>วันที่ประเมิน:</strong> <span id="modalEvalDate">-</span>
      </div>

      <form id="evaluationForm">
        <!-- Teacher Verbal Behaviors -->
        <div class="eval-section">
          <h4 class="section-title">ครู - พฤติกรรมด้านภาษา (Verbal Behaviors)</h4>
          <div class="question-list">
            <div class="question-item">
              <label class="question-label">1. ครูมีการให้ข้อมูลแก่นักเรียนสม่ำเสมอตลอดการจัดการเรียนรู้</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q1" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>

            <div class="question-item">
              <label class="question-label">2. ครูมีการถามคำถามแก่นักเรียนสม่ำเสมอตลอดการจัดการเรียนรู้</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q2" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>

            <div class="question-item">
              <label class="question-label">3. ครูมีการให้คำแนะนำแก่นักเรียนสม่ำเสมอตลอดการจัดการเรียนรู้</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q3" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>

            <div class="question-item">
              <label class="question-label">4. ครูมีการว่ากล่าวตักเตือนนักเรียนสม่ำเสมอตลอดการจัดการเรียนรู้</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q4" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>
          </div>
        </div>

        <!-- Teacher Non-Verbal Behaviors -->
        <div class="eval-section">
          <h4 class="section-title">ครู - พฤติกรรมที่ไม่ใช่ภาษา (Non-Verbal Behavior)</h4>
          <div class="question-list">
            <div class="question-item">
              <label class="question-label">5. ครูมีการแสดงท่าทาง การเคลื่อนไหว ความเงียบ</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q5" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>

            <div class="question-item">
              <label class="question-label">6. ครูมีการยิ้ม การหัวเราะ</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q6" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>

            <div class="question-item">
              <label class="question-label">7. ครูมีการมอง</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q7" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>

            <div class="question-item">
              <label class="question-label">8. ครูมีการเดิน</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q8" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>

            <div class="question-item">
              <label class="question-label">9. ครูมีการเขียนกระดาน/ เขียนบอร์ด/ เขียนบนหน้าจอ</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q9" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>
          </div>
        </div>

        <!-- Student Academic Behaviors -->
        <div class="eval-section">
          <h4 class="section-title">นักเรียน - พฤติกรรมทางวิชาการของผู้เรียน</h4>
          <div class="question-list">
            <div class="question-item">
              <label class="question-label">1. นักเรียนเพ่งมองในสิ่งที่ครูกำลังดำเนินการสอน</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q10" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>

            <div class="question-item">
              <label class="question-label">2. นักเรียนทำงานตามที่ครูมอบหมายในชั้นเรียน</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q11" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>

            <div class="question-item">
              <label class="question-label">3. นักเรียนสนทนากันในชั้นเรียน</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q12" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>

            <div class="question-item">
              <label class="question-label">4. นักเรียนไม่เพ่งมองสิ่งที่ครูกำลังดำเนินการสอน</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q13" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>

            <div class="question-item">
              <label class="question-label">5. นักเรียนไม่ทำงานตามที่ครูมอบหมายในชั้นเรียน</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q14" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>

            <div class="question-item">
              <label class="question-label">6. นักเรียนพูดคุยกันในระหว่างครูสอน</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q15" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>
          </div>
        </div>

        <!-- Student Work Behaviors -->
        <div class="eval-section">
          <h4 class="section-title">นักเรียน - พฤติกรรมการทำงานของผู้เรียน</h4>
          <div class="question-list">
            <div class="question-item">
              <label class="question-label">1. นักเรียนตั้งใจทำงาน</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q16" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>

            <div class="question-item">
              <label class="question-label">2. นักเรียนไม่ตั้งใจทำงานและไม่รบกวนผู้อื่น</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q17" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>

            <div class="question-item">
              <label class="question-label">3. นักเรียนไม่ตั้งใจทำงานแต่รบกวนผู้อื่น</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q18" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>
          </div>
        </div>

        <!-- Learning Environment -->
        <div class="eval-section">
          <h4 class="section-title">สิ่งแวดล้อมทางการเรียนรู้ - สภาพทางกายภาพของห้องเรียน</h4>
          <div class="question-list">
            <div class="question-item">
              <label class="question-label">1. พื้นห้องเรียนมีรอยเปื้อนและสกปรก</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q19" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>

            <div class="question-item">
              <label class="question-label">2. เครื่องคอมพิวเตอร์มีอุปกรณ์ต่อพ่วงเพียงพอต่อการเรียน เช่น แป้นพิมพ์ เมาส์ เป็นต้น และใช้งานได้</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q20" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>

            <div class="question-item">
              <label class="question-label">3. ฝาผนังมีรอยเปื้อนและสะอาดเป็นบางส่วน</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q21" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>

            <div class="question-item">
              <label class="question-label">4. มีเครื่องคอมพิวเตอร์ใช้งานอย่างเพียงพอ 1 เครื่องต่อนักเรียน 1 คน</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q22" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>

            <div class="question-item">
              <label class="question-label">5. เครื่องคอมพิวเตอร์สามารถใช้งานได้ทุกเครื่อง</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q23" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>

            <div class="question-item">
              <label class="question-label">6. ในห้องเรียนคอมพิวเตอร์มีอุปกรณ์ในการสอน เช่น ทีวี เครื่องและจอฉาย เครื่องขยายเสียง เป็นต้น</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q24" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>

            <div class="question-item">
              <label class="question-label">7. ห้องเรียนคอมพิวเตอร์มีแสงสว่างมากจนเกินไป</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q25" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>

            <div class="question-item">
              <label class="question-label">8. ห้องเรียนคอมพิวเตอร์มีแสงสว่างไม่เพียงพอ</label>
              <div class="rating-group">
                <% for (let i = 1; i <= 5; i++) { %>
                  <label class="rating-option">
                    <input type="radio" name="q26" value="<%= i %>" required>
                    <span><%= i %></span>
                  </label>
                <% } %>
              </div>
            </div>
          </div>
        </div>

        <div class="rating-legend" style="background:var(--color-bg);padding:12px;border-radius:6px;margin-top:24px;text-align:center">
          <strong>เกณฑ์การให้คะแนน:</strong>
          <span style="margin-left:16px">1 = ปรับปรุง</span>
          <span style="margin-left:8px">|</span>
          <span style="margin-left:8px">2 = พอใช้</span>
          <span style="margin-left:8px">|</span>
          <span style="margin-left:8px">3 = ปานกลาง</span>
          <span style="margin-left:8px">|</span>
          <span style="margin-left:8px">4 = ดี</span>
          <span style="margin-left:8px">|</span>
          <span style="margin-left:8px">5 = มากที่สุด</span>
        </div>
      </form>
    </div>

    <div class="modal-footer">
      <button type="button" class="btn btn--secondary" onclick="closeEvaluationModal()">
        ยกเลิก
      </button>
      <button type="button" class="btn btn--primary" onclick="submitEvaluation()" <%= interactiveEnabled ? '' : 'disabled title="ไม่สามารถส่งได้ โปรดเลือกการฝึกประสบการณ์วิชาชีพครูที่ใช้งานก่อน"' %>>
        ส่งแบบประเมิน
      </button>
    </div>
  </div>
</div>

<link rel="stylesheet" href="/css/evaluation.css">
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="/js/observation-utils.js"></script>
<script>
// Expose server-side config to evaluation.js
window.evalPageConfig = {
  isPureAdmin: <%= isPureAdmin ? 'true' : 'false' %>,
  hasPractice: <%= hasPractice ? 'true' : 'false' %>,
  hasActive: <%= hasActive ? 'true' : 'false' %>,
  userYear: <%= userYear %>
};
</script>
<script src="/js/evaluation.js"></script>
